overwrite: true
schema: "src/**/*.graphql"
documents: null
generates:
  src/generated/resolvers-types.ts:
    config:
      useIndexSignature: true
    plugins:
      - typescript
      - typescript-resolvers
